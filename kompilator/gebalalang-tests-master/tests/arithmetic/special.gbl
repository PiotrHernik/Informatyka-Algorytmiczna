#Bartek Łabuz
#pragma test in(100, 0) out(0, 0, 0, 100, 100)
PROGRAM IS
    t, s, u, v, w, p, q
BEGIN
    READ t;
    READ s;
    u := t / s;
    v := t % s;
    w := t * s;
    p := t + s;
    q := t - s;
    WRITE u;
    WRITE v;
    WRITE w;
    WRITE p;
    WRITE q;

END